# Backend API base URL.
# - Production builds use .env.production (the deployed Render backend).
# - In dev, leave this empty to use the Vite proxy (which also targets the Render backend
#   by default), or set it to http://localhost:8000 to develop against a local backend.
VITE_API_BASE=https://meeting-intelligence-platform.onrender.com
